# Relay GPIO pin (BCM numbering)
RELAY_GPIO_PIN=17

# Demo mode — simulate relay + manual temperature input (no hardware needed)
DEMO_MODE=false

# Target platform: "rpi" or "dev" (used by setup scripts)
TARGET=dev
